{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build enumType",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "-o",
                "enumType",
                "enumtype.c"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}